import React from "react";
import styled from "styled-components";

const SearchingHotel = () => {
  return (
    <Wrapper>
      <div className="main">Searching Hotel</div>
    </Wrapper>
  );
};

const Wrapper = styled.section``;

export default SearchingHotel;
