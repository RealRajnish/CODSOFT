import React from "react";
import styled from "styled-components";

const SearchingFlight = () => {
  return (
    <Wrapper>
      <div className="main">Searching Flight</div>
    </Wrapper>
  );
};

const Wrapper = styled.section``;

export default SearchingFlight;
